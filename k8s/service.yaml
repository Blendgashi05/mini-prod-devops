apiVersion: v1
kind: Service
metadata:
  name: mini-app-service
  namespace: mini-prod
spec:
  type: NodePort
  selector:
    app: mini-app
    version: blue
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30080
